name: Simple CI

on:
  push:

jobs:
  build-html:
    name: Build HTML Job
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: List files
        run: ls -la
