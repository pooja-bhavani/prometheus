name: Hello World

on: push # Triggers the workflow when you push the changes

jobs:
  hello-job:           #job 1
    runs-on: ubuntu-latest

    steps:
      - name: Say Hello
        run: echo "Hello, World! 🚀"

  test-job:            # job 2
    runs-on: ubuntu-latest
    steps:
      - name: test hello-world
        run: echo "Running tests ✅"

